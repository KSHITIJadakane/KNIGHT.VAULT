services:
  - type: web
    name: knight-vault-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_PROOF_SERVER_URI
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
